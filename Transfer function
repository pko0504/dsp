num=[1 2];
den=[1 3 2];
N=50;
imp_signal=[1; zeros(N-1,1)];
imp_response=filter(num,den,imp_signal);
figure;
stem(0:N-1,imp_response,'b','linewidth',1.5);
title('impulse response');
xlabel('n');
ylabel('amplitude');
grid on;
